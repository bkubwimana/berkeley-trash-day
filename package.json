{
  "name": "berkeley-trash-day",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A privacy-preserving, community-reported trash collection day tracker for Berkeley, California.",
  "scripts": {
    "dev": "npx --yes netlify-cli@23.15.1 dev",
    "generate:service-areas": "node scripts/generate-service-areas.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "check": "vite build && node --check scripts/generate-service-areas.mjs && node --check src/service-areas.generated.mjs && node --check src/service-area-geometries.generated.mjs && node --check src/calendar.mjs && node --check src/corridor.mjs && node --check src/map-data.mjs && node --check src/reporting.mjs && node --check vite.config.js && node --check netlify/functions/schedule.mjs && node --check netlify/functions/submit-report.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^10.0.0",
    "maplibre-gl": "^6.8.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
